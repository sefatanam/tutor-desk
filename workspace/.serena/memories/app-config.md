Current app configuration: Angular web app with modern UI redesign completed. Uses dual framework: Tailwind CSS v4 + PrimeNG v21 with Aura preset. Design updated with sharper corners (2px → 8px), reduced spacing, compact sidebar (220px vs 280px), shorter header (52px vs 64px), square logo, and left-border active state. API configuration: Go REST API at http://localhost:8080/api/v1 with proxy configured for development.